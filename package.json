{
  "name": "scoreflow-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --only firestore --project scoreflow-6059f \"node tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.5",
    "firebase-tools": "^13.29.1"
  }
}
