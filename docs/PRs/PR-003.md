# PR-003 — Establish ScoreFlow Coach Design System

## Status

Ready for local testing and review.

## Branch

`agent/pr-003-coach-design-system`

## Purpose

Establish the visual and responsive foundation for ScoreFlow Coach before functional modules are built. The design follows the Coach Milestones core direction: volleyball-first, coach-focused, mobile and iPad primary, premium ScoreFlow styling, thumb-friendly controls, and minimal interface clutter.

## Scope

- Replace the generic light foundation with a dark premium sports workspace.
- Build a useful Coach Hub dashboard using representative placeholder data.
- Establish ScoreFlow red and gold action hierarchy.
- Establish reusable color, spacing, radius, panel, button, card, status, and typography tokens.
- Use a compact navigation rail for iPad and desktop.
- Use persistent bottom navigation for primary phone actions.
- Keep the application shell confined to `100dvh`.
- Restrict scrolling to the route content region instead of the full page.
- Support safe-area insets for installed iPhone and iPad PWAs.
- Add height-specific compression for short landscape viewports.
- Preserve every existing route as a clearly styled placeholder.
- Do not connect Firebase or build working team, roster, match, rotation, or reporting features yet.
- Do not modify the existing scoreboard app.

## Files Changed

- `ScoreFlow-Coach/src/App.tsx`
- `ScoreFlow-Coach/src/styles.css`
- `docs/PRs/PR-003.md`

## Implementation Notes

The main application frame uses a fixed-height grid with `height: 100dvh`, `min-height: 0`, and hidden outer overflow. The top bar and navigation remain fixed within the app frame. Only `.route-viewport` scrolls when a device cannot display all dashboard content at once.

The responsive strategy starts with iPad-sized navigation and then adapts downward. At phone widths, the primary five sections and a More control appear in a bottom navigation bar. Secondary destinations remain available through the slide-out menu.

The Coach Hub communicates the planned product direction without introducing persistent data. It includes a next-match card, quick actions, practice plan, last-match metrics, local-save status language, and links to the existing placeholder routes.

No external icon package was added. The temporary symbols are lightweight placeholders so the design system can be tested before permanent ScoreFlow icons are selected.

## Testing Checklist

- [ ] Pull `agent/pr-003-coach-design-system` locally.
- [ ] From `ScoreFlow-Coach`, run `npm install`.
- [ ] Run `npm run typecheck`.
- [ ] Run `npm run build`.
- [ ] Run `npm run dev`.
- [ ] Confirm the Coach Hub loads without console errors.
- [ ] Confirm the dark styling feels consistent across every route.
- [ ] Confirm the app frame does not create document-level horizontal scrolling.
- [ ] Confirm only the route content area scrolls vertically when required.
- [ ] Test phone portrait at approximately 390 × 844.
- [ ] Test phone landscape at approximately 844 × 390.
- [ ] Test iPad portrait at approximately 820 × 1180.
- [ ] Test iPad landscape at approximately 1180 × 820.
- [ ] Confirm phone bottom navigation remains visible and usable.
- [ ] Confirm the More button opens the complete navigation drawer.
- [ ] Confirm the drawer closes after selecting a route.
- [ ] Confirm iPad navigation rail remains compact and readable.
- [ ] Confirm short-height landscape rules keep cards and controls usable.
- [ ] Confirm safe-area padding works in installed PWA mode where available.
- [ ] Confirm all routes still work after the redesign.
- [ ] Confirm the existing root scoreboard remains unchanged.

## Known Limitations

- Dashboard data is representative placeholder content only.
- Navigation symbols are temporary and will be replaced by the final icon set.
- Firebase, authentication, offline data storage, and scoreboard integration are not included.
- Dashboard cards are not yet customizable.
- Detailed accessibility testing with screen readers remains a later validation step.

## Merge Notes

Merge only after successful typecheck, production build, and real or emulated phone/iPad testing. After merge, the next recommended milestone is the Teams, Clubs, and Seasons foundation using this design system.
