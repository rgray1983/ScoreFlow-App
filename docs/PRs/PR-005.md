# PR-005 — Players, Rosters, Context Switching, Slide-Over Editor, and Custom Controls

## Status

Ready for local testing and review.

## Branch

`agent/pr-005-players-rosters-slide-over`

## Purpose

Introduce reusable player profiles and season-specific roster memberships while establishing shared context selection, right-side editing, and custom form-control patterns for future Coach modules.

## Scope

- Add strict TypeScript models for players and roster memberships.
- Keep reusable player profiles separate from season roster details.
- Add local-first player and roster persistence to the existing workspace store.
- Migrate older local workspace records safely when player arrays are missing.
- Add representative player and roster seed data.
- Replace the Players placeholder with a working directory for the active organization, team, and season.
- Add player creation and editing.
- Store jersey number, position, status, captain, libero, and starter flags per season roster.
- Update the Coach Hub with the active roster count.
- Add a reusable `ContextBar` for changing organization, team, and season without leaving the current page.
- Show static context labels when only one option exists and selectors when switching is possible.
- Add a reusable `SlideOver` component.
- Use a right-side eased entrance, fading blur backdrop, and mobile full-width behavior.
- Add reusable `SelectField` and `DateField` controls so browser-native white menus and calendars do not break the ScoreFlow visual system.
- Use the custom select in the context bar, program forms, and player editor.
- Use the custom calendar for season start and end dates.
- Preserve required date validation, keyboard focus states, outside-click closing, and compact phone-landscape layouts.
- Preserve the existing scoreboard and all PR-004 program structure behavior.

## Context Interaction

The Players page treats Organization → Team → Season as shared application context rather than navigation. Changing a selector immediately updates the active workspace, player directory, roster count, and new-player destination. A compact Manage link returns to Teams only when the user needs to create or change the underlying program structure.

When only one option exists, the bar shows its name as a compact label instead of wasting space on an unnecessary dropdown. The component is reusable for Schedule, Live Match, Rotations, Reports, Attendance, and future context-aware modules.

## Shared Control Interaction

`SelectField` renders a dark translucent ScoreFlow menu with selected-state feedback instead of the operating system dropdown. `DateField` renders a dark month calendar with previous/next navigation, today and clear actions, selected and current-day states, and a compact landscape arrangement. Both controls reuse hidden form values so existing `FormData` save workflows remain unchanged.

## Slide-Over Interaction

The editor mounts off-screen at the right edge, then enters using a smooth `cubic-bezier(.16, 1, .3, 1)` transition with a small scale settle. The backdrop fades and blurs separately. Closing reverses the transition before the component unmounts.

On phones the editor becomes full-width. In short phone landscape it becomes wider, reduces header and field height, uses four columns where practical, and hides the optional long notes field so primary player and roster data remain in the viewport.

## Local Testing

```bash
git fetch origin
git switch agent/pr-005-players-rosters-slide-over
git pull
cd ScoreFlow-Coach
npm install
npm run typecheck
npm run build
npm run dev
```

## Testing Checklist

- [ ] Confirm PR-004 organization, team, and season records still load.
- [ ] Switch organizations, teams, and seasons from Players.
- [ ] Confirm custom context menus stay inside the viewport and are not clipped by cards.
- [ ] Confirm selected options show the ScoreFlow selected state.
- [ ] Open Add Program and confirm Program Type uses the custom menu.
- [ ] Open Add Season and confirm both dates use the custom dark calendar.
- [ ] Confirm month navigation, Today, Clear, selected day, and current-day states work.
- [ ] Confirm required season dates still prevent an incomplete save.
- [ ] Open Add/Edit Player and test all custom dropdowns.
- [ ] Add a player and assign jersey number, position, status, and role flags.
- [ ] Refresh and confirm player and roster persistence.
- [ ] Confirm the Hub roster count updates.
- [ ] Confirm the slide-over enters and exits smoothly from the right.
- [ ] Test phone portrait, phone landscape, iPad portrait, and iPad landscape.
- [ ] Confirm the existing root scoreboard remains unchanged.

## Deferred

- Firebase and authentication
- Player photos and uploads
- Contact information and portal permissions
- Duplicate jersey validation
- Drag-and-drop lineup or bench order
- Availability, attendance, injuries, recruiting, and statistics
- Dedicated Roster route and bulk roster tools
- Applying `ContextBar`, `SelectField`, and `DateField` to future modules as those modules are implemented
