# PR-016 — Rebuild Look, Ports, and Local Test Workflow

## Status

Ready for local testing and review.

## Branch

`cursor/rebuild-look-and-local-test-feac`

## Purpose

Lock look-and-feel, side-by-side local ports, and the test-then-merge workflow in `docs/SCOREBOARD_REBUILD.md` before Phase 1 code starts.

## Scope

- Keep the current ScoreFlow visual language; record polish rules (button hierarchy, LED scoreboard, live pill, SVG icons).
- Lock local ports: current app `4173`, new Vite app `5173`.
- Require a **Run in VS Code** command block on every rebuild PR.
- Record that Richie tests locally, requests adjustments, then merges before the next phase.
- No application code changes.

## Files Changed

- `docs/SCOREBOARD_REBUILD.md`
- `docs/PRs/PR-016.md`

## Implementation Notes

This is still Phase 0. Phase 1 (Vite + scoring kernel) waits until this PR is merged.

## Testing Checklist

- [ ] Read the Look and feel section and confirm it keeps the current brand.
- [ ] Confirm current app command is `npx --yes serve -l 4173 .`
- [ ] Confirm new app port is 5173 and must not replace 4173.
- [ ] Confirm no `app.js` / `index.html` / Firebase files changed.

## Known Limitations

Until Phase 1, `npm test` and `npm run dev` do not exist yet. Only the current-app command runs today.

## Merge Notes

After merge, the next PR is Phase 1: Vite + TypeScript + `src/scoring` with Vitest.
