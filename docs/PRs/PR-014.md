# PR-014 — Remove ScoreFlow-Coach Folder

## Status

Ready for local testing and review.

## Branch

`cursor/remove-scoreflow-coach-folder-feac`

## Purpose

Remove the nested `ScoreFlow-Coach` React application from this repository so the repo contains only the live ScoreFlow scoreboard.

## Scope

- Delete the `ScoreFlow-Coach` folder and all files inside it.
- Leave the root scoreboard app, Firebase config, and product documentation unchanged.

## Files Changed

- `ScoreFlow-Coach/**` (removed)
- `docs/PRs/PR-014.md`

## Implementation Notes

`ScoreFlow-Coach` was a separate Vite + React + TypeScript workspace. Historical PR records that mention that folder remain as-is. Product vision docs that describe ScoreFlow Coach as a future product were not rewritten.

## Testing Checklist

- [ ] Confirm `ScoreFlow-Coach` is gone from the working tree.
- [ ] Open the root `index.html` scoreboard and confirm it still loads.
- [ ] Confirm no remaining imports or scripts point at `ScoreFlow-Coach`.

## Known Limitations

Past PR records still mention `cd ScoreFlow-Coach` as historical implementation notes.

## Merge Notes

Merge after confirming the root scoreboard is the only app remaining in the repository.
