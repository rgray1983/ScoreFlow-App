# PR-015 — Scoreboard Rebuild Plan

## Status

Ready for local testing and review.

## Branch

`cursor/scoreboard-rebuild-plan-feac`

## Purpose

Lock the scoreboard rebuild before any implementation. This repo is the volleyball scoreboard. Coach stays in its own repo and is out of scope.

## Scope

- Add `docs/SCOREBOARD_REBUILD.md` as the implementation map: stack, architecture, routes, scoring kernel, live data, phase order, and cutover.
- Point `docs/README.md` at that file.
- No application code changes.

## Files Changed

- `docs/SCOREBOARD_REBUILD.md`
- `docs/README.md`
- `docs/PRs/PR-015.md`

## Implementation Notes

The current `index.html` / `app.js` app remains the live product until the Cutover phase. The first code phase after this document is the TypeScript scoring kernel and Vite toolchain, not a screen rewrite.

## Testing Checklist

- [ ] Read `docs/SCOREBOARD_REBUILD.md` and confirm Coach is out of scope.
- [ ] Confirm the first code phase is toolchain + scoring tests, not UI.
- [ ] Confirm no `app.js` / `index.html` / Firebase files changed.

## Known Limitations

Older Coach vision docs in this repo were not rewritten. The rebuild plan says to ignore them when they conflict with scoreboard work.

## Merge Notes

After merge, the next PR should be Phase 1: Vite + TypeScript + `src/scoring` with Vitest.
